---
id: m-2
title: "M3 - Workflow Prompts (Ingest, Query, Lint)"
---

## Description

Expose the Ingest/Query/Lint skill workflows as MCP prompts so the semantics travel with the server, not per-host system prompts. Outcome: any MCP client can invoke `ingest`/`query`/`lint` prompts and get the same behavior the current Andy skill produces. Doc: doc-2 (Wiki Structure), existing/skill.md.
